{"config":{"lang":["en"],"separator":"[\\s\\-\\.]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"install/","title":"Installation","text":"<p>TODO: one line on install paths.</p>"},{"location":"install/#library","title":"Library","text":"<p>TODO: install as a package, import as <code>pipe</code>. Requires Python &gt;= 3.11.</p> <pre><code>pip install pipe-broker\n</code></pre> <pre><code>import pipe\n\npipe.echo(\"hi\")\n</code></pre>"},{"location":"install/#serve-the-api","title":"Serve the API","text":"<p>TODO: how to run the FastAPI adapter locally and in a container.</p> <pre><code># local dev (defaults to 0.0.0.0:8787)\npython -m pipe.api.dev\n</code></pre> <p>Info</p> <p>TODO: note on <code>PIPE_ROOT_PATH</code> for reverse-proxy prefixes, and <code>PIPE_PUBLIC_DIR</code> for the static frontend location.</p>"},{"location":"install/#configuration","title":"Configuration","text":"Env var Purpose Default <code>PORT</code> TODO <code>8080</code> <code>PIPE_HOST</code> / <code>PIPE_PORT</code> TODO dev bind <code>0.0.0.0</code> / <code>8787</code> <code>PIPE_ROOT_PATH</code> TODO reverse-proxy prefix (empty) <code>PIPE_PUBLIC_DIR</code> TODO static frontend dir repo <code>/public</code> <code>OPENROUTER_API_KEY</code> TODO LLM + STT (unset) <code>AVIATIONSTACK_API_KEY</code> TODO flights provider (unset)"},{"location":"programming-model/","title":"Programming model","text":"<p>TODO: one line \u2014 Pipe reproduces DSPy\u2019s split of a structural layer and a behavioural layer.</p>"},{"location":"programming-model/#pipe","title":"Pipe","text":"<p>TODO: a <code>Pipe</code> is the atomic callable. Subclasses implement <code>forward</code> (sync), and optionally <code>aforward</code> (async) and <code>stream</code> (incremental). One calling convention: <code>__call__</code> \u2192 forward, <code>acall</code> \u2192 aforward, <code>astream</code> \u2192 stream. Never call <code>forward</code> directly.</p> <pre><code>class Upper(pipe.Pipe):\n    def forward(self, data: str) -&gt; str:\n        return data.upper()\n\nUpper()(\"hi\")  # 'HI'\n</code></pre>"},{"location":"programming-model/#chain","title":"Chain","text":"<p>TODO: sequencing lives only in <code>Chain</code>. Compose with <code>&gt;&gt;</code> (or <code>.then(...)</code>); chaining flattens, so <code>a &gt;&gt; b &gt;&gt; c</code> is one chain of three. Output of each stage feeds the next.</p> <pre><code>pipeline = DetectPII(\"us\") &gt;&gt; Redact()\npipeline(\"call me at 555-123-4567\")\n</code></pre>"},{"location":"programming-model/#ambient-context","title":"Ambient context","text":"<p>TODO: cross-cutting request data (locale, jurisdiction, currency, lat/lon) travels ambiently through a <code>contextvars</code> stack instead of every signature. Enter it with a <code>with</code> block.</p> <pre><code>with pipe.Pipe.context(LegalRequestContext(jurisdiction=\"us/ny\")):\n    result = detect(text)\n</code></pre>"},{"location":"programming-model/#prediction-envelope","title":"Prediction envelope","text":"<p>TODO: every domain pipe speaks one currency \u2014 a <code>Prediction</code> wrapping a <code>Document</code> and a list of <code>Detection</code>s. Uniform in, uniform out, so ops compose freely.</p>"},{"location":"api/","title":"API Reference","text":"<p>TODO: two ways to use pipe \u2014 import it as a library, or serve that same library as an HTTP API on your own cloud. This section documents both.</p> <ul> <li> <p>Library</p> <p>TODO: the importable surface \u2014 core <code>Pipe</code>/<code>Chain</code>, ops, experts, providers, utils.</p> <p> Library</p> </li> <li> <p>REST API</p> <p>TODO: serve any op as an HTTP endpoint on your own infrastructure \u2014 same core, no rewrite. You own the keys, data boundary, and deployment.</p> <p> REST API</p> </li> </ul>"},{"location":"api/library/","title":"Library","text":"<p>TODO: the importable surface. <code>import pipe</code> for the flat entry points; the richer capabilities live under <code>pipe.lib.*</code>. This is where the capability detail lives.</p>"},{"location":"api/library/#core","title":"Core","text":"<p>TODO: <code>Pipe</code>, <code>Chain</code>, and the flat helpers (<code>echo</code>, <code>echo_stream</code>).</p> <pre><code>import pipe\n\npipe.echo(\"hi\")\n</code></pre>"},{"location":"api/library/#experts","title":"Experts","text":"<p>TODO: <code>pipe.lib.experts</code> \u2014 the domain-expertise ops. Legal today (<code>DetectPII</code>, <code>SpotClauses</code>, <code>ExtractCitations</code>, <code>IdentifyStatutes</code>, <code>ScreenPrivilege</code>, <code>Redact</code>); more domains to come.</p>"},{"location":"api/library/#providers","title":"Providers","text":"<p>TODO: <code>pipe.lib.providers</code> \u2014 external-service-wrapping pipes (<code>OpenRouterProvider</code>, <code>STT</code>, <code>FxRates</code>, <code>Weather</code>, <code>Geocode</code>, <code>Flights</code>, <code>SatelliteEmbedding</code>).</p>"},{"location":"api/library/#utilities","title":"Utilities","text":"<p>TODO: <code>pipe.lib.utils</code> \u2014 pure pipes for finance, geo, search relevance, and flow combinators.</p>"},{"location":"api/library/#shared-types","title":"Shared types","text":"<p>TODO: <code>pipe.lib.shared</code> \u2014 the <code>Prediction</code> envelope, <code>Jurisdiction</code>, <code>Scope</code>, <code>Locale</code>, and request-context types.</p>"},{"location":"api/rest/","title":"REST API \u2014 serve the library on your own cloud","text":"<p>TODO: the selling point. Every pipe op you can <code>import</code> can also be served as an HTTP API on your own infrastructure \u2014 no rewrite. The same pure core runs as a thin, stateless FastAPI adapter you deploy under your own cloud account. Your data and keys never leave your environment.</p> <p>Why serve it yourself</p> <p>TODO: turn any pipe op into an endpoint your services can call, on the go. You own the deployment, the keys, the data boundary, and the bill \u2014 we don\u2019t proxy your traffic. Run it at the edge (Cloudflare Python Worker) or as a container (Cloud Run, ECS, or any box that runs Python).</p>"},{"location":"api/rest/#the-idea","title":"The idea","text":"<p>TODO: the library is the API. <code>pipe.api</code> is a thin adapter over <code>pipe.lib</code> \u2014 each handler parses input, calls one atomic op, and returns the output. Because the core is pure and stateless, the same code serves a laptop, a container, or an edge worker. \u201cSame core, no rewrite.\u201d</p>"},{"location":"api/rest/#serve-it","title":"Serve it","text":"<p>TODO: one command locally; a container image for production.</p> <pre><code># local\npython -m pipe.api.dev            # 0.0.0.0:8787\n\n# production ASGI entrypoint (binds $PORT, default 8080)\npython -m pipe.api.server\n</code></pre> <p>TODO: note the config knobs \u2014 <code>PIPE_ROOT_PATH</code> (reverse-proxy prefix), <code>PIPE_PUBLIC_DIR</code> (static site), <code>PORT</code>, <code>PIPE_HOST</code>/<code>PIPE_PORT</code>, and the provider keys (<code>OPENROUTER_API_KEY</code>, <code>AVIATIONSTACK_API_KEY</code>) \u2014 all read from your environment.</p>"},{"location":"api/rest/#routes","title":"Routes","text":"<p>TODO: the endpoints exposed by the adapter. Interactive OpenAPI at <code>/docs</code>, raw spec at <code>/openapi.json</code>.</p>"},{"location":"api/rest/#ops","title":"Ops","text":"Method Path Style Params Returns <code>GET</code> <code>/echo</code> buffered <code>data</code> <code>{ data }</code> <code>GET</code> <code>/echo/stream</code> streaming <code>data</code>, <code>chunk_size</code> <code>text/plain</code> stream"},{"location":"api/rest/#llm-speech","title":"LLM &amp; speech","text":"Method Path Style Params Returns <code>POST</code> <code>/llm</code> buffered <code>prompt</code>, <code>model</code>, <code>max_tokens</code> <code>{ model, prompt, completion }</code> <code>POST</code> <code>/stt</code> buffered <code>file</code> (multipart), <code>model</code>, <code>language</code> <code>{ model, format, text }</code>"},{"location":"api/rest/#legal-expert","title":"Legal expert","text":"Method Path Style Params Returns <code>GET</code> <code>/legal/ops</code> buffered \u2014 <code>{ operations, categories, rule_count }</code> <code>POST</code> <code>/legal/detect</code> buffered <code>text</code>, <code>jurisdiction</code>, <code>op</code> <code>{ op, jurisdiction, detections, meta }</code> <p>Info</p> <p>TODO: <code>/llm</code> and <code>/stt</code> return shaped 501/503/502 errors when the stack or a key is unavailable, so they are safe to probe.</p>"},{"location":"api/rest/#deploy-targets","title":"Deploy targets","text":"<p>TODO: describe the supported deploy targets.</p> <ul> <li>Edge \u2014 Cloudflare Python Worker (Pyodide); pure-Python ops run at the edge.</li> <li>Container \u2014 the shipped <code>Dockerfile</code> runs on Cloud Run, ECS, Fly, or any   container host; real CPython, so any dependency works.</li> <li>Anywhere Python runs \u2014 it\u2019s just an ASGI app.</li> </ul>"},{"location":"diving-deeper/architecture/","title":"Architecture","text":"<p>TODO (KT): the big picture. How the pieces fit \u2014 the pure <code>pipe.lib</code> core, the <code>pipe.api</code> serving adapter, and why they are separated (same core as a library or served at the edge, no rewrite).</p>"},{"location":"diving-deeper/architecture/#layers","title":"Layers","text":"<p>TODO (KT): describe the layering, borrowed from DSPy.</p> <ul> <li>Structural layer \u2014 <code>BasePipe</code> / <code>PipeMeta</code>: how pipes are discovered,   named, and deep-copied (<code>named_pipes</code>, <code>pipes</code>, <code>deepcopy</code>).</li> <li>Behavioural layer \u2014 <code>Pipe</code>: the calling convention   (<code>__call__</code>/<code>acall</code>/<code>astream</code>) and composition (<code>then</code> / <code>&gt;&gt;</code>).</li> <li>Serving layer \u2014 <code>pipe.api</code>: thin async handlers over the core.</li> </ul>"},{"location":"diving-deeper/architecture/#package-map","title":"Package map","text":"Package Responsibility <code>pipe.lib.core</code> TODO: BasePipe, Pipe, Chain, context, ops <code>pipe.lib.shared</code> TODO: types, jurisdiction, scope, registry, locale, context <code>pipe.lib.detectors</code> TODO: pattern-driven detection <code>pipe.lib.experts</code> TODO: domain expertise (generic, workflow, legal) <code>pipe.lib.providers</code> TODO: external-service-wrapping pipes <code>pipe.lib.utils</code> TODO: pure finance / geo / search / flow pipes <code>pipe.api</code> TODO: FastAPI app, routes, server, dev"},{"location":"diving-deeper/architecture/#design-tenets","title":"Design tenets","text":"<ol> <li>TODO (KT): atomic over monolithic \u2014 one op does one thing.</li> <li>TODO (KT): sequencing lives only in <code>Chain</code>.</li> <li>TODO (KT): cross-cutting data travels ambiently, not through signatures.</li> <li>TODO (KT): one uniform envelope (<code>Prediction</code>) so ops compose.</li> </ol>"},{"location":"diving-deeper/context-and-scope/","title":"Ambient context, jurisdiction &amp; scope","text":"<p>TODO (KT): the cross-cutting model that makes ops stay atomic \u2014 how locale, jurisdiction, currency, and coordinates reach an op without being passed through every call.</p>"},{"location":"diving-deeper/context-and-scope/#requestcontext","title":"RequestContext","text":"<p>TODO (KT): <code>contextvars</code>-based stack (thread- and task-local). <code>use_context</code>, <code>current_context</code>, <code>require_context(kind)</code>. Subtypes: <code>FinancialRequestContext</code> (currency), <code>LegalRequestContext</code> (jurisdiction), <code>GeoRequestContext</code> (lat/lon).</p> <pre><code>with pipe.Pipe.context(LegalRequestContext(jurisdiction=\"us/ny\")):\n    result = detect(text)\n</code></pre>"},{"location":"diving-deeper/context-and-scope/#jurisdiction","title":"Jurisdiction","text":"<p>TODO (KT): hierarchical territorial path + orthogonal facets (<code>us/wa/king/seattle</code>, facets like <code>domain</code>, <code>personal_law</code>). <code>contains</code> / <code>is_within</code> prefix containment; <code>ANY</code> is universal.</p>"},{"location":"diving-deeper/context-and-scope/#scope-the-registry","title":"Scope &amp; the registry","text":"<p>TODO (KT): how <code>Scope</code> + <code>ScopedRegistry</code> resolve rules by specificity <code>(depth, #facets, authority)</code> with lex-specialis dedup \u2014 the most specific applicable rule wins (e.g. NY\u2019s non-compete overriding the US default).</p>"},{"location":"diving-deeper/context-and-scope/#authority","title":"Authority","text":"<p>TODO (KT): <code>Authority</code> ordering (LOCAL &lt; SUBNATIONAL &lt; NATIONAL &lt; SUPRANATIONAL &lt; CONSTITUTIONAL) and where it enters resolution.</p>"},{"location":"diving-deeper/extending/","title":"Extending Pipe","text":"<p>TODO (KT): the most important KT page \u2014 how a new maintainer adds capability without breaking the model. Recipes, not theory.</p>"},{"location":"diving-deeper/extending/#add-a-new-op","title":"Add a new op","text":"<p>TODO (KT): subclass <code>Pipe</code>, implement <code>forward</code> (and <code>aforward</code>/<code>stream</code> if needed), keep it pure. What to return.</p> <pre><code>class Upper(pipe.Pipe):\n    def forward(self, data: str) -&gt; str:\n        return data.upper()\n</code></pre>"},{"location":"diving-deeper/extending/#add-a-detector","title":"Add a detector","text":"<p>TODO (KT): register <code>Rule</code>s and use <code>RegexDetector</code> / <code>KeywordDetector</code>. Detectors carry no domain knowledge \u2014 they are patterns fed in.</p>"},{"location":"diving-deeper/extending/#add-a-legal-rule-jurisdiction","title":"Add a legal rule / jurisdiction","text":"<p>TODO (KT): register a <code>Rule</code> under a <code>Scope</code> + <code>Authority</code> in the legal <code>catalog</code>; how lex-specialis override works (e.g. refining a national rule at the state level). How to add a new jurisdiction path or facet.</p>"},{"location":"diving-deeper/extending/#add-a-provider","title":"Add a provider","text":"<p>TODO (KT): subclass <code>ProviderPipe</code>; read keys from env (never log them; <code>repr</code> shows <code>has_key</code> only); lazy-import the client; provide sync <code>forward</code> + async <code>aforward</code>. How to fail honestly when creds/deps are missing (the <code>SatelliteEmbedding</code> scaffold pattern).</p>"},{"location":"diving-deeper/extending/#add-an-expert-workflow","title":"Add an expert / workflow","text":"<p>TODO (KT): compose existing detectors/transforms into a named <code>Workflow</code> (a <code>Chain</code> subclass) with <code>describe()</code>. \u201cA legal workflow is nothing more than a Workflow of generic detectors/transforms configured with legal patterns.\u201d</p>"},{"location":"diving-deeper/extending/#expose-it-over-http","title":"Expose it over HTTP","text":"<p>TODO (KT): add a thin async handler in <code>pipe.api.routes</code> that parses input, calls the op, and returns output. Keep buffered vs. streaming explicit. This is how a new op joins the REST API.</p>"},{"location":"diving-deeper/pipes-and-chains/","title":"Pipes and chains in depth","text":"<p>TODO (KT): the mechanics behind the programming model \u2014 the level of detail a new maintainer needs.</p>"},{"location":"diving-deeper/pipes-and-chains/#the-pipe-lifecycle","title":"The Pipe lifecycle","text":"<p>TODO (KT): <code>PipeMeta.__call__</code> runs <code>Pipe._base_init</code> before a subclass <code>__init__</code>, so <code>name</code> always exists. Why the metaclass, and what invariants it guarantees.</p>"},{"location":"diving-deeper/pipes-and-chains/#calling-convention","title":"Calling convention","text":"<p>TODO (KT): why you never call <code>forward</code> directly.</p> Call Runs Notes <code>pipe(x)</code> <code>forward</code> sync <code>await pipe.acall(x)</code> <code>aforward</code> defaults to running <code>forward</code> <code>pipe.astream(x)</code> <code>stream</code> incremental"},{"location":"diving-deeper/pipes-and-chains/#composition-and-flattening","title":"Composition and flattening","text":"<p>TODO (KT): how <code>&gt;&gt;</code> / <code>then</code> build a <code>Chain</code>, and how chaining flattens so <code>a &gt;&gt; b &gt;&gt; c</code> is a single three-stage chain (not nested). How <code>Chain.forward</code>, <code>aforward</code>, and <code>stream</code> execute stages (all-but-last via <code>acall</code>, tail streamed).</p> <pre><code>pipeline = DetectPII(\"us\") &gt;&gt; Redact()\n</code></pre>"},{"location":"diving-deeper/pipes-and-chains/#nested-pipes-and-discovery","title":"Nested pipes and discovery","text":"<p>TODO (KT): how <code>named_pipes()</code> walks one level of <code>__dict__</code> (lists, tuples, dicts) to discover sub-pipes with dotted/indexed names like <code>stages[0]</code>.</p>"},{"location":"diving-deeper/prediction-envelope/","title":"The Prediction envelope","text":"<p>TODO (KT): the single data structure every domain pipe reads and writes, and why a uniform envelope is what lets ops compose freely.</p>"},{"location":"diving-deeper/prediction-envelope/#types","title":"Types","text":"<p>TODO (KT): the frozen dataclasses in <code>pipe.lib.shared.types</code> (stdlib only).</p> Type Role <code>Severity</code> TODO: INFO \u2026 CRITICAL ordering <code>Span</code> TODO: start/end offsets <code>Detection</code> TODO: kind, label, span, severity, meta <code>Document</code> TODO: text + meta + locale (<code>with_text</code>, <code>with_locale</code>) <code>Prediction</code> TODO: document + detections; the output envelope"},{"location":"diving-deeper/prediction-envelope/#reading-and-writing","title":"Reading and writing","text":"<p>TODO (KT): <code>Prediction.of()</code> coercion, <code>with_document</code>, <code>add_detections</code>, <code>by_kind</code>, <code>max_severity</code>. How a detector adds detections and a transform (e.g. <code>Redactor</code>, <code>Annotator</code>, <code>SeverityGate</code>) reads them without mutating text destructively.</p> <pre><code>pred = DetectPII(\"us\")(\"SSN 123-45-6789\")\npred.by_kind(\"pii.ssn\")\npred.max_severity()\n</code></pre>"},{"location":"tutorials/","title":"Tutorials","text":"<p>TODO: overview of the tutorials \u2014 short, task-focused walkthroughs that build real things with pipe. Mirrors DSPy\u2019s tutorials layout.</p> <ul> <li> <p>Your first op</p> <p>TODO: call <code>echo</code>, then write your own <code>Pipe</code>.</p> <p> First op</p> </li> <li> <p>Streaming</p> <p>TODO: incremental output with <code>echo_stream</code> and <code>astream</code>.</p> <p> Streaming</p> </li> <li> <p>Composing a pipeline</p> <p>TODO: chain ops with <code>&gt;&gt;</code> and pass ambient context.</p> <p> Composing</p> </li> <li> <p>A legal pipeline</p> <p>TODO: detect and redact PII for a jurisdiction, end to end.</p> <p> Legal pipeline</p> </li> </ul>"},{"location":"tutorials/composing/","title":"Composing a pipeline","text":"<p>TODO: put ops together with <code>&gt;&gt;</code>, and supply cross-cutting data via ambient context.</p>"},{"location":"tutorials/composing/#chain-ops","title":"Chain ops","text":"<pre><code>pipeline = DetectPII(\"us\") &gt;&gt; Redact()\npipeline(\"call me at 555-123-4567\")\n</code></pre> <p>TODO: how chaining flattens; output of each stage feeds the next.</p>"},{"location":"tutorials/composing/#ambient-context","title":"Ambient context","text":"<pre><code>with pipe.Pipe.context(LegalRequestContext(jurisdiction=\"us/ny\")):\n    result = pipeline(text)\n</code></pre> <p>TODO: why context beats threading arguments through every op.</p>"},{"location":"tutorials/first-op/","title":"Your first op","text":"<p>TODO: the \u201chello world\u201d \u2014 call the built-in <code>echo</code>, then write a <code>Pipe</code>.</p>"},{"location":"tutorials/first-op/#call-a-built-in-op","title":"Call a built-in op","text":"<pre><code>import pipe\n\npipe.echo(\"hi\")  # 'hi'\n</code></pre>"},{"location":"tutorials/first-op/#write-your-own","title":"Write your own","text":"<p>TODO: subclass <code>Pipe</code>, implement <code>forward</code>, call it. Keep it pure.</p> <pre><code>class Upper(pipe.Pipe):\n    def forward(self, data: str) -&gt; str:\n        return data.upper()\n\nUpper()(\"hi\")  # 'HI'\n</code></pre> <p>TODO: next steps \u2014 link to Streaming and Composing.</p>"},{"location":"tutorials/legal-pipeline/","title":"A legal pipeline","text":"<p>TODO: an end-to-end example \u2014 detect and redact PII for a jurisdiction.</p> <p>Warning</p> <p>TODO: the legal patterns are pragmatic starting points, not legal advice.</p>"},{"location":"tutorials/legal-pipeline/#detect-then-redact","title":"Detect, then redact","text":"<pre><code>from pipe.lib.experts.legal import DetectPII, Redact\n\npipeline = DetectPII(\"us\") &gt;&gt; Redact()\npipeline(\"SSN 123-45-6789\")\n</code></pre> <p>TODO: walk through what each op emits (the <code>Prediction</code> envelope), how <code>Redact</code> masks already-detected spans, and how switching the jurisdiction (e.g. <code>us/ny</code>) changes the rules via lex-specialis resolution.</p>"},{"location":"tutorials/legal-pipeline/#over-http","title":"Over HTTP","text":"<p>TODO: when served as an API, the same op via <code>POST /legal/detect</code> with <code>op=detect_pii</code>. See the REST API.</p>"},{"location":"tutorials/streaming/","title":"Streaming","text":"<p>TODO: produce output incrementally instead of buffering.</p>"},{"location":"tutorials/streaming/#the-streaming-op","title":"The streaming op","text":"<pre><code>import pipe\n\nasync for chunk in pipe.echo_stream(\"stream me\", chunk_size=2):\n    print(chunk, end=\"\")\n</code></pre> <p>TODO: how <code>astream</code> maps to a <code>Pipe.stream</code> generator, and how a <code>Chain</code> streams its tail while running earlier stages buffered.</p>"},{"location":"tutorials/streaming/#over-http","title":"Over HTTP","text":"<p>TODO: when served as an API, <code>GET /echo/stream</code> returns a <code>text/plain</code> stream; show a small client reading it chunk-by-chunk. See the REST API.</p>"}]}